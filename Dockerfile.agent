FROM python:3.11-slim

WORKDIR /app

COPY . .

RUN pip install --no-cache-dir -r requirements.txt

# Default to remote mode inside Docker
ENV ENV_MODE=remote
ENV BASE_URL=http://env:8000/v1

CMD ["python", "-m", "runners.agent_runner"]