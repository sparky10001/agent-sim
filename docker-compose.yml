# docker/docker-compose.yml

services:
  agent-sim:
    build: .
    container_name: agent-sim
    ports:
      - "8000:8000"
    environment:
      - LOG_DIR=/app/logs/runs  
    volumes:
      - ~/agent-sim/runs:/app/runs
      - ~/agent-sim/logs:/app/logs